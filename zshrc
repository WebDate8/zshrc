alias k='kubectl'
alias lll='lsb_release -a’
alias i='ip -c -br a'
alias ls='exa'
alias gh='history|grep'
alias rm='rm -rf'
alias mkdir='mkdir -pv'
alias fs='du -sh * | sort -h' #FileSize
alias upd='apt update && apt upgrade -y'
alias df='df -h'
alias c='clear'
alias h='history'
alias ports='netstat -tulanp'
alias top='btop'
alias G='apt install'
alias rr='python3 ~/ranger/ranger.py'
alias nn='ncdu' #Ncdu is a disk usage analyzer with an ncurses interface.
#It is designed to find space hogs on a remote server where
#you don_t have an entire graphical setup available,
#but it is a useful tool even on regular desktop systems.
#Ncdu aims to be fast, simple and easy to use, and should be
#able to run in any minimal POSIX-like environment with ncurses installed
